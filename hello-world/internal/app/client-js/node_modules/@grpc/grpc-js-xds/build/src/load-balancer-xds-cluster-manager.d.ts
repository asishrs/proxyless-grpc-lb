import { experimental } from "@grpc/grpc-js/";
import LoadBalancingConfig = experimental.LoadBalancingConfig;
interface ClusterManagerChild {
    child_policy: LoadBalancingConfig[];
}
export declare class XdsClusterManagerLoadBalancingConfig implements LoadBalancingConfig {
    private children;
    getLoadBalancerName(): string;
    constructor(children: Map<string, ClusterManagerChild>);
    getChildren(): Map<string, ClusterManagerChild>;
    toJsonObject(): object;
    static createFromJson(obj: any): XdsClusterManagerLoadBalancingConfig;
}
export declare function setup(): void;
export {};
