import { StatusObject } from "@grpc/grpc-js";
import { Listener__Output } from "../generated/envoy/api/v2/Listener";
import { RdsState } from "./rds-state";
import { Watcher, XdsStreamState } from "./xds-stream-state";
export declare class LdsState implements XdsStreamState<Listener__Output> {
    private rdsState;
    private updateResourceNames;
    versionInfo: string;
    nonce: string;
    private watchers;
    private latestResponses;
    constructor(rdsState: RdsState, updateResourceNames: () => void);
    addWatcher(targetName: string, watcher: Watcher<Listener__Output>): void;
    removeWatcher(targetName: string, watcher: Watcher<Listener__Output>): void;
    getResourceNames(): string[];
    private validateResponse;
    private handleMissingNames;
    handleResponses(responses: Listener__Output[]): string | null;
    reportStreamError(status: StatusObject): void;
}
